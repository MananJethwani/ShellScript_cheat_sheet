# use > to store output in a particular file
echo "to store in file.txt">file.txt
# to store input from terminal to a specific file use cat
cat>file.txt
# use >> to append instead of rewriting the file
